# Web3Forms access key used by the contact form (src/App.tsx).
# Optional: the app falls back to a built-in key if this is unset.
# This value is exposed to the browser at build time — do not put secrets here.
# Copy to .env and override, or set VITE_WEB3FORMS_KEY in CI.
VITE_WEB3FORMS_KEY=
